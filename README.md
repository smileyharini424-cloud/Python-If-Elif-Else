# Python If Elif Else

## Explanation

Conditional statements allow a Python program to make decisions based on conditions.

The `if`, `elif`, and `else` statements are used to execute different blocks of code depending on whether a condition is true or false.

## Problem Statement

Write a Python program that accepts marks from the user and displays the student's result based on the marks.

## Features

* Accepts marks from the user
* Checks different mark ranges
* Uses `if`, `elif`, and `else`
* Displays the corresponding result

## How It Works

The program checks the marks using multiple conditions:

* Marks 90 or above → Grade A
* Marks 75 to 89 → Grade B
* Marks 50 to 74 → Grade C
* Marks below 50 → Fail

## Technologies Used

* Python 3

## Program Flow

1. Start the program.
2. Read marks from the user.
3. Check the marks using `if`.
4. If false, check the conditions using `elif`.
5. If none of the conditions match, execute `else`.
6. Display the result.
7. End the program.

## Sample Input

```text
Enter marks: 82
```

## Sample Output

```text
Grade B
```

## Key Learning

* Understanding conditional statements
* Using `if`, `elif`, and `else`
* Creating decision-making logic
* Comparing numerical values

## File Location

```text
Python-If-Elif-Else/if_elif_else.py
```

## Repository Structure

```text
Python-If-Elif-Else/
│
├── if_elif_else.py
└── README.md
```

## Author

V.Harini
